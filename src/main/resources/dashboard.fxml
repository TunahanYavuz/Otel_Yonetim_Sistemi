<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<VBox spacing="30" styleClass="dashboard-content" stylesheets="@style.css"
      xmlns="http://javafx.com/javafx/21.0.5" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="ymt_odev.Controllers.DashboardController">
    <padding>
        <Insets top="30" bottom="30" left="40" right="40"/>
    </padding>

    <!-- Hoşgeldin Mesajı -->
    <VBox spacing="10" styleClass="welcome-section">
        <Text styleClass="welcome-title" text="Hoşgeldiniz! 👋"/>
        <Text fx:id="welcomeMessage" styleClass="welcome-text"
              text="Otel yönetim sisteminize başarıyla giriş yaptınız."/>
    </VBox>

    <!-- İstatistikler -->
    <HBox alignment="CENTER" spacing="20">
        <!-- Müsait Odalar -->
        <VBox alignment="CENTER" spacing="10" styleClass="stat-card" HBox.hgrow="ALWAYS">
            <padding>
                <Insets top="20" bottom="20" left="30" right="30"/>
            </padding>
            <Text styleClass="stat-icon" text="🛏️"/>
            <Text styleClass="stat-title" text="Müsait Odalar"/>
            <Text fx:id="availableRoomsCount" styleClass="stat-number" text="45"/>
        </VBox>

        <!-- Aktif Rezervasyonlar -->
        <VBox alignment="CENTER" spacing="10" styleClass="stat-card" HBox.hgrow="ALWAYS">
            <padding>
                <Insets top="20" bottom="20" left="30" right="30"/>
            </padding>
            <Text styleClass="stat-icon" text="📅"/>
            <Text styleClass="stat-title" text="Aktif Rezervasyonlar"/>
            <Text fx:id="activeReservationsCount" styleClass="stat-number" text="23"/>
        </VBox>

        <!-- Toplam Müşteri -->
        <VBox alignment="CENTER" spacing="10" styleClass="stat-card" HBox.hgrow="ALWAYS">
            <padding>
                <Insets top="20" bottom="20" left="30" right="30"/>
            </padding>
            <Text styleClass="stat-icon" text="👥"/>
            <Text styleClass="stat-title" text="Toplam Müşteri"/>
            <Text fx:id="totalCustomersCount" styleClass="stat-number" text="156"/>
        </VBox>
    </HBox>

    <!-- Hızlı İşlemler -->
    <VBox spacing="15">
        <Text styleClass="section-title" text="⚡ Hızlı İşlemler"/>

        <HBox alignment="CENTER" spacing="15">
            <Button text="➕ Yeni Rezervasyon" onAction="#showNewReservation"
                    styleClass="action-button" prefWidth="180"/>
            <Button text="🔍 Oda Ara" onAction="#switchToRoomSearch"
                    styleClass="action-button" prefWidth="180"/>
            <Button text="✅ Check-in Yap" onAction="#showCheckin"
                    styleClass="action-button" prefWidth="180"/>
            <Button text="🚪 Check-out Yap" onAction="#showCheckout"
                    styleClass="action-button" prefWidth="180"/>
        </HBox>
    </VBox>

    <!-- Son İşlemler -->
    <VBox spacing="15" VBox.vgrow="ALWAYS">
        <Text styleClass="section-title" text="📋 Son İşlemler"/>

        <TableView fx:id="recentActivitiesTable" styleClass="modern-table" VBox.vgrow="ALWAYS">
            <columns>
                <TableColumn fx:id="timeColumn" text="Zaman" prefWidth="120"/>
                <TableColumn fx:id="actionColumn" text="İşlem" prefWidth="150"/>
                <TableColumn fx:id="descriptionColumn" text="Açıklama" prefWidth="300"/>
                <TableColumn fx:id="statusColumn" text="Durum" prefWidth="100"/>
            </columns>
        </TableView>
    </VBox>

    <!-- Alt Bilgi -->
    <HBox alignment="CENTER_RIGHT">
        <Text fx:id="currentDateTime" styleClass="datetime-text" text="29 Kasım 2025 - 23:45"/>
    </HBox>

</VBox>
