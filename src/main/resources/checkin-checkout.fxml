<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<VBox spacing="20" styleClass="dashboard-content" stylesheets="@style.css"
      xmlns="http://javafx.com/javafx/21.0.5" xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="ymt_odev.Controllers.CheckinCheckoutController">
    <padding>
        <Insets top="20" bottom="20" left="20" right="20"/>
    </padding>

    <!-- Başlık -->
    <HBox alignment="CENTER_LEFT" spacing="15">
        <Text text="✅" styleClass="hotel-icon" style="-fx-font-size: 32px;"/>
        <Text text="Check-in / Check-out İşlemleri" styleClass="main-title"/>
        <Region HBox.hgrow="ALWAYS"/>
        <Button text="🏠 Ana Menü" styleClass="action-button" onAction="#goToMainMenu"
                style="-fx-background-color: linear-gradient(to bottom, #95a5a6, #7f8c8d);"/>
    </HBox>

    <Separator/>

    <!-- İşlem Seçimi -->
    <HBox spacing="30" alignment="CENTER" styleClass="form-container">
        <padding>
            <Insets top="20" bottom="20" left="30" right="30"/>
        </padding>

        <VBox spacing="15" alignment="CENTER" styleClass="stat-card" HBox.hgrow="ALWAYS"
              style="-fx-cursor: hand;">
            <padding>
                <Insets top="30" bottom="30" left="40" right="40"/>
            </padding>
            <Text text="✅" styleClass="stat-icon" style="-fx-font-size: 48px;"/>
            <Text text="Check-in" styleClass="section-title"/>
            <Text text="Misafir girişi yapın" styleClass="info-detail"/>
            <Button fx:id="checkInButton" text="Check-in Yap" styleClass="action-button" prefWidth="150" onAction="#performCheckIn"/>
        </VBox>

        <VBox spacing="15" alignment="CENTER" styleClass="stat-card" HBox.hgrow="ALWAYS"
              style="-fx-cursor: hand;">
            <padding>
                <Insets top="30" bottom="30" left="40" right="40"/>
            </padding>
            <Text text="🚪" styleClass="stat-icon" style="-fx-font-size: 48px;"/>
            <Text text="Check-out" styleClass="section-title"/>
            <Text text="Misafir çıkışı yapın" styleClass="info-detail"/>
            <Button fx:id="checkOutButton" text="Check-out Yap" styleClass="action-button" prefWidth="150" onAction="#performCheckOut"
                    style="-fx-background-color: linear-gradient(to bottom, #e74c3c, #c0392b);"/>
        </VBox>
    </HBox>

    <!-- Rezervasyon Arama -->
    <VBox spacing="15" styleClass="form-container">
        <padding>
            <Insets top="20" bottom="20" left="25" right="25"/>
        </padding>

        <Text text="🔍 Rezervasyon Ara" styleClass="section-title"/>

        <HBox spacing="15" alignment="CENTER_LEFT">
            <VBox spacing="5" HBox.hgrow="ALWAYS">
                <Text text="Rezervasyon No veya Müşteri Bilgisi:" styleClass="info-text"/>
                <TextField fx:id="customerSearchField" promptText="Rezervasyon No, TC Kimlik veya E-posta..."
                           styleClass="modern-textfield"/>
            </VBox>

            <Button text="🔍 Ara" styleClass="action-button" onAction="#searchReservation">
                <VBox.margin>
                    <Insets top="20"/>
                </VBox.margin>
            </Button>
        </HBox>
    </VBox>

    <!-- Bugünün Check-in/Check-out Listesi -->
    <TabPane VBox.vgrow="ALWAYS" tabClosingPolicy="UNAVAILABLE" styleClass="modern-table">

        <!-- Bugün Check-in Yapacaklar -->
        <Tab text="📥 Bugün Check-in Yapacaklar">
            <VBox spacing="10">
                <padding>
                    <Insets top="15" bottom="15" left="15" right="15"/>
                </padding>

                <TableView styleClass="modern-table" VBox.vgrow="ALWAYS">
                    <columns>
                        <TableColumn text="Rezervasyon No" fx:id="reservationIdColumn" prefWidth="120"/>
                        <TableColumn text="Müşteri" fx:id="customerNameColumn" prefWidth="180"/>
                        <TableColumn text="Oda No"/>
                        <TableColumn text="Giriş Saati" prefWidth="100"/>
                        <TableColumn text="Gün Sayısı" prefWidth="100"/>
                        <TableColumn text="Durum" fx:id="statusColumn" prefWidth="100"/>
                        <TableColumn text="İşlemler" prefWidth="150"/>
                    </columns>
                </TableView>
            </VBox>
        </Tab>

        <!-- Bugün Check-out Yapacaklar -->
        <Tab text="📤 Bugün Check-out Yapacaklar">
            <VBox spacing="10">
                <padding>
                    <Insets top="15" bottom="15" left="15" right="15"/>
                </padding>

                <TableView styleClass="modern-table" VBox.vgrow="ALWAYS">
                    <columns>
                        <TableColumn text="Rezervasyon No" prefWidth="120"/>
                        <TableColumn text="Müşteri" prefWidth="180"/>
                        <TableColumn text="Oda No"/>
                        <TableColumn text="Çıkış Saati" prefWidth="100"/>
                        <TableColumn text="Toplam Tutar" fx:id="totalPriceColumn" prefWidth="120"/>
                        <TableColumn text="Durum" prefWidth="100"/>
                        <TableColumn text="İşlemler" prefWidth="150"/>
                    </columns>
                </TableView>
            </VBox>
        </Tab>

        <!-- Şu An Otelde Kalanlar -->
        <Tab text="🏨 Şu An Otelde Kalanlar">
            <VBox spacing="10">
                <padding>
                    <Insets top="15" bottom="15" left="15" right="15"/>
                </padding>

                <TableView styleClass="modern-table" VBox.vgrow="ALWAYS">
                    <columns>
                        <TableColumn text="Oda No"/>
                        <TableColumn text="Müşteri" prefWidth="180"/>
                        <TableColumn text="Giriş Tarihi" fx:id="checkInColumn" prefWidth="120"/>
                        <TableColumn text="Çıkış Tarihi" fx:id="checkOutColumn" prefWidth="120"/>
                        <TableColumn text="Kalan Gün" prefWidth="100"/>
                        <TableColumn text="Toplam Tutar" prefWidth="120"/>
                        <TableColumn text="İşlemler" prefWidth="150"/>
                    </columns>
                </TableView>
            </VBox>
        </Tab>

    </TabPane>

</VBox>
