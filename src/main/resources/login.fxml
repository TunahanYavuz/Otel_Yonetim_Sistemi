<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.*?>

<AnchorPane prefHeight="600.0" prefWidth="800.0" styleClass="main-container"
            stylesheets="@style.css" xmlns="http://javafx.com/javafx/21.0.5"
            xmlns:fx="http://javafx.com/fxml/1" fx:controller="ymt_odev.Controllers.AuthController">

    <!-- Ana Container -->
    <VBox alignment="CENTER" spacing="30" AnchorPane.topAnchor="0" AnchorPane.bottomAnchor="0"
          AnchorPane.leftAnchor="0" AnchorPane.rightAnchor="0" styleClass="login-container">

        <padding>
            <Insets top="80" bottom="80" left="100" right="100"/>
        </padding>

        <!-- Başlık -->
        <VBox alignment="CENTER" spacing="10">
            <Text text="🏨" styleClass="hotel-icon" />
            <Text text="Otel Yönetim Sistemi" styleClass="main-title" />
            <Text text="Giriş Yapın" styleClass="subtitle" />
        </VBox>

        <!-- Giriş Formu -->
        <VBox alignment="CENTER" spacing="20" styleClass="form-container">
            <padding>
                <Insets top="30" bottom="30" left="40" right="40"/>
            </padding>

            <!-- Kullanıcı Tipi Seçimi -->
            <HBox alignment="CENTER" spacing="20">
                <fx:define>
                    <ToggleGroup fx:id="userTypeGroup"/>
                </fx:define>

                <RadioButton text="👤 Müşteri" toggleGroup="$userTypeGroup" selected="true"
                           fx:id="customerRadio" styleClass="user-type-radio"/>
                <RadioButton text="👔 Personel" toggleGroup="$userTypeGroup"
                           fx:id="staffRadio" styleClass="user-type-radio"/>
            </HBox>

            <!-- Giriş Alanları -->
            <VBox alignment="CENTER" spacing="15" maxWidth="300">
                <TextField fx:id="username" promptText="📧 E-posta veya TC Kimlik"
                          styleClass="modern-textfield" maxWidth="300"/>
                <PasswordField fx:id="password" promptText="🔒 Şifre"
                              styleClass="modern-textfield" maxWidth="300"/>
            </VBox>

            <!-- Şifremi Unuttum -->
            <Hyperlink text="Şifremi Unuttum" styleClass="forgot-password" />
        </VBox>

        <!-- Butonlar -->
        <HBox alignment="CENTER" spacing="20">
            <Button text="🚪 Giriş Yap" onAction="#handleLogin"
                   styleClass="primary-button" prefWidth="150"/>
            <Button text="📝 Kayıt Ol" onAction="#switchRegister"
                   styleClass="secondary-button" prefWidth="150"/>
        </HBox>

        <!-- Alt Bilgi -->
        <VBox alignment="CENTER" spacing="5">
            <Text text="Güvenli Giriş Sistemi" styleClass="footer-text"/>
            <Text text="© 2025 Otel Yönetim Sistemi v1.0" styleClass="version-text"/>
        </VBox>

    </VBox>
</AnchorPane>
